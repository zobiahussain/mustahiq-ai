# workflows — Trigger Layer

LlamaIndex Workflow definitions for the 8 event triggers + 1 scheduled sweep. Cross-cutting
— see [docs/Team_Work_Division.md §3](../docs/Team_Work_Division.md#3-trigger-ownership)
for per-trigger ownership. Wired together last, once the functions each trigger calls are
stable (see build order).
