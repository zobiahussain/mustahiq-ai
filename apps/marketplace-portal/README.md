# apps/marketplace-portal — Marketplace Portal

**Owner:** Marketplace, Business Matching & Shared RAG Layer role.

React + Vite, separate codebase from `apps/main-portal`. Store listing creation, ranked
match results with reasons and premium listings marked, alerts on new matches.

**Depends on:** `services/api`.

Shares palette, logo, and typography with `apps/main-portal` despite being a separate
codebase.
