# Local dev uses the Vite proxy to the staff API.
VITE_STAFF_API_BASE=/portal

# If the frontend is hosted away from the API, point this at the backend prefix.
# VITE_STAFF_API_BASE=https://<api-host>/portal
